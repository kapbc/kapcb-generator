server:
  port: 9096

spring:
  application:
    name: @artifactId@
  datasource:
    hikari:
      jdbc-url: jdbc:mysql://localhost:3306/user_grp?characterEncoding=UTF-8&serverTimezone=UTC&useUnicode=true
      username: root
      password: 123456
      driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  type-aliases-package: com.kapcb.ccc.domain.po